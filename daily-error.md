1.要缩短工程总工期，**必须同时覆盖并缩短这 3 条关键路径**。



2.题目：指令系统设计与扩展操作码计算

> 假设某计算机的指令字长为 16 位，系统设计支持四种指令格式：三地址指令、二地址指令、一地址指令和零地址指令。
>
> 每个地址码字段均占 4 位。
>
> 已知当前系统已经设计了：
>
> - **12** 条三地址指令
> - **44** 条二地址指令
> - **254** 条一地址指令
>
> **问题：** 在保证操作码前缀不重叠的前提下，该计算机最多还能设计出多少条 **零地址指令**？

解析与推导

我们在上文提到过一个核心递推公式：**$下一级可用指令数 = 上一级留出的状态数 \times 2^{借用的地址位数}$**。

我们按照指令操作数从多到少（操作码从短到长）的顺序逐层剥开：

**1. 基础操作码长度**

指令字长 16 位，如果是三地址指令，3 个地址码共占用 $3 \times 4 = 12$ 位。

因此，留给基础操作码（三地址操作码）的长度是：$16 - 12 = 4$ 位。

**2. 三地址指令层**

- 4 位操作码理论上最多可以表示 $2^4 = 16$ 种状态。
- 题目已知使用了 12 条三地址指令。
- **剩余状态数：** $16 - 12 = 4$ 种状态。（这 4 种状态将作为前缀，向下扩展）。

**3. 二地址指令层**

二地址指令比三地址指令少了一个 4 位的地址码，这 4 位被借用来扩展操作码。

- 理论上，上一级留下的 4 种状态，每一种都可以和这新借来的 4 位组合，产生出：$4 \times 2^4 = 64$ 种可能的状态。
- 题目已知使用了 44 条二地址指令。
- **剩余状态数：** $64 - 44 = 20$ 种状态。

**4. 一地址指令层**

一地址指令再次借用一个 4 位的地址码。

- 理论上，上一级留下的 20 种状态继续向下扩展，可以产生：$20 \times 2^4 = 320$ 种状态。
- 题目已知使用了 254 条一地址指令。
- **剩余状态数：** $320 - 254 = 66$ 种状态。

**5. 零地址指令层**

零地址指令没有地址码，所有的地址位全被借用作为操作码。

- 上一级留下的 66 种状态，再次与最后 4 位地址码组合扩展。
- 可以产生：$66 \times 2^4 = 1056$ 种状态。
- 因为零地址是最后一层，不需要再为更少的地址指令保留扩展窗口了，所以这 1056 种状态可以**全部**用来表示零地址指令。

**最终答案：** 该计算机最多还能设计出 **1056** 条零地址指令。



3.题目：已知底层的指令条数反推顶层

> 假设某计算机指令字长为 16 位，系统设计支持四种指令格式：三地址、二地址、一地址和零地址指令。
>
> 每个地址码字段均占 4 位。
>
> 现已知该指令系统中，设计了：
>
> - **64** 条零地址指令
> - **252** 条一地址指令
> - **48** 条二地址指令
>
> **问题：** 该系统最多还能设计出多少条 **三地址指令**？

解析与推导

我们依然使用那个核心递推关系，但在逆向推导时，我们要用它的变形： **上一层留下的状态数 = (本层已用指令数 + 留给下一层的状态数) $\div 2^4$**

> **关键认知：** 每一层产生的总状态数（无论是用来设计本层指令，还是留给下一层扩展），都必须是 $2^4 = 16$ 的整数倍。因为它是上一层留下来的状态乘以 16 得来的。

我们从最底层（零地址）开始往上逆推：

1. 从零地址推一地址（逆向第一步）

- 零地址指令由于是最后一层，不再向下扩展，所以总共有 **64** 种状态。
- 这 64 种状态是由上一层（一地址层）留下的状态扩展而来的。扩展系数是 $2^4 = 16$。
- **一地址层留下的状态数** = $64 \div 16 = 4$ 种状态。

2. 从一地址推二地址（逆向第二步）

- 一地址层本身使用了 **252** 条指令。
- 一地址层同时还要留下 4 种状态给零地址层。
- 一地址层**总共需要的状态数** = $252 + 4 = 256$ 种状态。
- 这 256 种状态是由更上一层（二地址层）扩展而来的。
- **二地址层留下的状态数** = $256 \div 16 = 16$ 种状态。

3. 从二地址推三地址（逆向第三步）

- 二地址层本身使用了 **48** 条指令。
- 二地址层还要留下 16 种状态给一地址层。
- 二地址层**总共需要的状态数** = $48 + 16 = 64$ 种状态。
- 这 64 种状态是由最顶层（三地址层）扩展而来的。
- **三地址层留下的状态数** = $64 \div 16 = 4$ 种状态。

4. 计算三地址指令（最终步）

- 三地址指令拥有原始的 4 位操作码，其总容量为 $2^4 = 16$ 种状态。
- 它需要扣除掉留给下一层的 4 种状态。
- **最终答案：** 最多还能设计 $16 - 4 =$ **12** 条三地址指令。



4.ISA规定的内容



5.堆栈零地址的运算类指令在指令格式中不给出操作数地址，参加的两个操作数来自于



6.**原理**：CPU 的时钟周期长度由数据通路中**最长路径的延迟**决定。

**对比**：

- **单周期 CPU**：时钟周期必须强制匹配**最复杂、耗时最长的指令**（如 `lw` 指令，必须包含取指 $\rightarrow$ 译码 $\rightarrow$ 计算地址 $\rightarrow$ 读内存 $\rightarrow$ 写寄存器完整全过程）。
- **多周期 CPU**：时钟周期仅取决于**最长的一个执行阶段**（如单次访存或单次 ALU 操作的时间）。



7.**原理**：

- **单周期 CPU**：整条指令在一个时钟周期内执行完毕。组合逻辑控制器根据指令操作码译码后，输出的各个控制信号在整个周期内**保持恒定不变**，直到下一个时钟沿切换新指令。
- **多周期 CPU**：指令分步执行，控制单元（通常基于有限状态机 FSM）在不同的时钟周期处于不同的状态，输出不同的控制信号组合（例如前一周期使能存储器读，后一周期使能寄存器写）。因此控制信号在一条指令的执行过程中**会随着阶段变化而发生改变**。



8.**原理**：部件复用的前提是**分时使用**。

**对比**：

- **单周期 CPU**：所有组合逻辑部件在同一个周期内协同工作，信号单向流动，**任何部件在一条指令中都不能被重复使用**（否则会导致逻辑竞争与冲突）。因此单周期必须使用独立的指令存储器和数据存储器，并配备多个加法器。
- **多周期 CPU**：可以在不同的时钟周期复用同一个部件（如同一个 ALU 可以在周期 1 用来算 $\text{PC}+4$，在周期 3 用来做算术运算）。



9.单总线结构的特点是 CPU 内部所有部件都挂在一根公共总线上，**同一时刻只能传输一个数据**。若要完成一条指令（如 $A + B \rightarrow C$），必须分步（分时复用）将数据依次送上总线，这需要**多个时钟周期**。

**结论**：单周期处理器要求**所有指令必须在 1 个时钟周期内完成**，因此**绝对不能采用单总线结构**，而必须采用专用数据通路（或多总线结构）以支持数据的并行传输。



10.数据通路中包含了**异常检测逻辑**（如 ALU 的溢出检测 `Overflow` 信号）以及**异常响应硬件**（如用于保存断点地址的 EPC 寄存器、保存异常原因的 Cause 寄存器，以及将 PC 强制跳转到异常入口地址的选通电路）。因此，数据通路**包含**这些电路。



11.法治监督体系是宪法法律有效实施的重要保障；法治保障体系是全面依法治国的重要依托



12.科学立法是全面依法治国的前提；公正司法是全面依法治国的看点；党的领导是中国特色社会主义最本质的特征，是社会主义法治最根本的保证；公正是法治的生命线，是司法活动的最高价值追求



13.法律至上表现为：普遍适用、优先适用、不可违反；法治思维表现为：法律至上、权力制约、公平正义、权力保障、程序正当



14.公平正义主要包括：权力公平、机会公平、规则公平、救济公平；权力公平：一是权力主体公平、而是享有的权力特别是基本权力平等、三是权力保护和权力救济平等



15.机会公平包括：起点平等、发展平等、代际平等



16.有些法律权力和法律义务具有复合性的关系，即一个行为可以同时是权力行为和义务行为



17.人身自由：人的身体不受拘束、人的行动自由、人身自由不受非法限制和剥夺



18.社会经济权力：劳动权、休息权、社会保障权、物质帮助权；财产权：私有财产权、继承权



19.反帝反封建的革命任务---是近代中国的历史任务；近代中国的基本国情---是认识中国近代一切社会问题和革命问题的最基本依据；资本帝国主义的侵略--是近代中国沦为半殖民地半封建社会的原因之一；民族资产阶级的软弱性---是近代中国民族资产阶级的两面性之一



20.近代中国社会进步和发展最大的障碍是帝国主义的侵略

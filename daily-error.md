1.

题目要求选出关于生成树说法**错误**的选项。

已知 $G = (V, E)$ 为无向连通图，$G' = (V', E')$ 为 $G$ 的生成树。根据数据结构中**图**的基本概念：

各选项分析

- **A. $G'$ 为 $G$ 的子图** —— **正确**
  - 生成树由图 $G$ 中的顶点和部分边构成，满足 $V' \subseteq V$ 且 $E' \subseteq E$，因此 $G'$ 必然是 $G$ 的子图。
- **B. $G'$ 为 $G$ 的连通分量** —— **错误（本题答案）**
  - **连通分量**的定义是无向图中的**极大连通子图**（含有该连通块内的所有顶点及所有边，无法再扩大）。
  - 而**生成树**是包含全部顶点的**极小连通子图**。生成树去掉了图中的回路/环，边数最少，并不满足“极大”的要求，因此生成树**不是**连通分量。
- **C. $G'$ 为 $G$ 的极小连通子图且 $V = V'$** —— **正确**
  - 包含图 $G$ 的全部顶点（$V = V'$），且保留最少边数（$n-1$ 条边）使图保持连通。再删去任意一条边都会导致图不连通，因此是**极小连通子图**。
- **D. $G'$ 是 $G$ 的一个无环子图** —— **正确**
  - 树的本质就是“连通且无环的图”。生成树作为子图，自然是一个无环子图。

核心概念对比记忆

| **概念**     | **对应子图类型** | **特点**                                                    |
| ------------ | ---------------- | ----------------------------------------------------------- |
| **连通分量** | **极大**连通子图 | 要求边数**最多**（包含该连通块内所有的边）                  |
| **生成树**   | **极小**连通子图 | 要求边数**最少**（$n$ 个顶点恰好有 $n-1$ 条边，无环且连通） |



2.SISD、SIMD、MIMD的概念

3.硬件多线程的基本概念

4.多核处理器的基本概念

5.共享内存处理器的基本概念



6.**程序计数器（PC）的核心职责**

CPU 在**取指令阶段**，指令的主存地址**始终来自于程序计数器（PC）**。PC 的唯一功能就是指示 CPU 下一条将要执行的指令所在的存储单元地址

**记忆要点**：**取指令看 PC，存取数据看寻址/MAR**。



7.转移指令包括**无条件转移**和**条件转移**：

- **无条件转移指令**：PC 的值一定会修改为转移目标地址。

- **条件转移指令**：只有在**转移条件满足**时，PC 的值才会修改为目标地址；如果**条件不满足**，程序会继续顺序执行（PC 保持之前自动自增后的下一条指令地址，不会被修改为转移目标地址）。

  因此，句中的“**总是**”过于绝对。



8.

- **I. 存储器的容量（正确）**

  程序计数器（PC）的核心功能是存放**下一条指令的主存地址**。因此，PC 的位数本质上就是**主存地址的位数**。主存容量决定了需要寻址的存储单元总数，容量越大，需要的地址位数（即 PC 的位数）就越多（例如，按字节编址的 4GB 主存需要 32 位地址，PC 即为 32 位）。

- **II. 机器字长（错误）**

  机器字长是指 CPU 一次能并行处理的二进制数据位数（通常决定了 ALU 和通用寄存器的宽度）。机器字长与地址位数没有直接绑定关联（例如早期的 8086 处理器机器字长为 16 位，但寻址地址线为 20 位）。

- **III. 指令字长（错误）**

  指令字长是指一条指令所包含的二进制代码位数。它决定的是**PC 在取指令后每次自动增加的步长**（例如单字长指令 $PC + 1$，双字长指令 $PC + 2$），而不决定 PC 本身用于表达主存地址的位数。

> **核心总结**
>
> - **PC 位数** $\to$ 看**主存容量 / 寻址范围**（决定存多少位地址）
> - **机器字长** $\to$ 看**数据处理能力 / ALU 宽度**（决定一次算多少位）
> - **指令字长** $\to$ 看**指令格式设计 / PC 自动自增步长**

9.间址周期（Indirect Address Cycle）

- **核心目的**：当指令采用**间接寻址**方式时，形式地址（指令中的地址码）存放的是有效地址的指针。该周期用于根据形式地址访问主存，取出操作数的**有效地址（EA）**。
- **触发条件**：仅在指令包含**间接寻址**标志时进入。
- **数据流向与微操作**：
  1. $Ad(IR) \to MAR$：将 $IR$ 中的形式地址（$Ad(IR)$）送到 $MAR$。
  2. $1 \to R$：发出主存读命令。
  3. $M(MAR) \to MDR \to Ad(IR)$（或送入暂存器）：读取有效地址，覆盖或更新原地址码。

10.中断周期（Interrupt Cycle）

- **核心目的**：在执行周期结束时，若 CPU 检测到硬件中断请求且满足响应条件，暂停当前程序，**保存断点**（如当前的 $PC$ 和状态寄存器 $PSW$），并转去执行中断服务程序。
- **触发条件**：执行周期结束时，硬件检测到未屏蔽的**中断请求**且 CPU 允许中断。
- **数据流向与微操作**（以堆栈保存断点为例）：
  1. $(SP) - 1 \to SP, (SP) \to MAR$：堆栈指针压栈，修改 $SP$ 并送入 $MAR$。
  2. $1 \to W$：发出写命令。
  3. $(PC) \to MDR \to M(MAR)$：将断点（$PC$ 内容）存入堆栈保护。
  4. 向量地址（或中断服务入口地址）$\to PC$：更新 $PC$，使下一周期跳转至中断服务程序执行。

11.空操作指令的指令周期中任何寄存器的内容都不会被改变（错误，符合题意）

即便是空操作指令（如 NOP），在**取指周期**中，CPU 同样需要将指令取出并更新状态：

- **程序计数器（$PC$）** 会递增，更新为下一条指令的地址（如 $(PC)+1 \to PC$）。

- **指令寄存器（$IR$）** 会存入空操作指令的代码。

- **地址寄存器（$MAR$）** 和 **数据寄存器（$MDR$）** 也会在访存过程中被改变。

  因此，“任何寄存器的内容都不会改变”的说法是不成立的。

```
[网格 DP (已完成)] 
       │
       ▼
[阶段一：背包专题] ───► 01背包 ──► 完全背包 ──► 凑零钱/目标和
       │
       ▼
[阶段二：双序列与线性 DP] ───► LIS ──► LCS ──► 编辑距离
       │
       ▼
[阶段三：状态机与约束 DP] ───► 打家劫舍/股票变体 ──► 附加限制路径 (对比去年真题)
       │
       ▼
[阶段四：树形 DP (高危)] ───► 树的最大路径和 ──► 树形打家劫舍 (C 指针+递归)
       │
       ▼
[阶段五：DAG 图上 DP] ───► 拓扑排序 + DP ──► 记忆化搜索 DFS
```





## 网格

1.在一个 $N \times M$ 的网格迷宫中，每个房间都放有一定数量的金币，金币数量由二维数组 `Grid[N][M]` 给出。一个探险家从左上角 `(0, 0)` 出发，目标是到达右下角 `(N-1, M-1)`。探险家每次**只能向右**或**向下**移动一步。

请设计一个算法，求出探险家到达终点时能收集到的最大金币总数。要求：

1. 写出算法的核心思想与状态转移方程。
2. 用伪代码实现该算法。
3. 分析算法的时间复杂度和空间复杂度。

```
Algorithm MaxCoinsPath(Grid, N, M):
    // 1. 数据结构初始化 (Initialization)
    定义一个大小为 N x M 的二维数组 dp[N][M]
    将 dp 数组的所有元素初始化为 0
    
    // 初始化起点
    dp[0][0] = Grid[0][0]
    
    // 初始化第一行 (仅受左边影响)
    FOR j = 1 TO M-1 DO:
        dp[0][j] = dp[0][j-1] + Grid[0][j]
    END FOR
    
    // 初始化第一列 (仅受上面影响)
    FOR i = 1 TO N-1 DO:
        dp[i][0] = dp[i-1][0] + Grid[i][0]
    END FOR
    
    // 2. 状态转移计算 (State Transition)
    // 从行索引 1 和 列索引 1 开始，计算剩余所有网格
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            // 比较从上方下来和从左方过来的金币数，取最大值并加上当前房间的金币
            dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + Grid[i][j]
        END FOR
    END FOR
    
    // 3. 返回最终结果
    RETURN dp[N-1][M-1]
```

**时间复杂度：** 算法使用两层嵌套循环遍历整个 $N \times M$ 网格，每次状态转移计算只需要常数时间 $O(1)$。因此，总时间复杂度为 **$O(N \times M)$**。

**空间复杂度：** 算法额外开辟了一个大小为 $N \times M$ 的二维数组 $dp$ 来存储中间状态。因此，空间复杂度为 **$O(N \times M)$**。（*注：如使用滚动数组进行空间优化，可将空间复杂度降为 $O(M)$*）





2.假设你要去参加研究生的复试，考场在顶楼。你面前有一段共有 $N$ 阶的楼梯。 由于你背着沉重的书包，你每次跨步**只能选择向上走 1 阶，或者走 2 阶**。 请问：你从平地（第 0 阶）爬到第 $N$ 阶，总共有多少种不同的走法？

```
Algorithm ClimbStairs(N):
    // 1. 边界处理
    IF N == 1 THEN RETURN 1
    IF N == 2 THEN RETURN 2
    
    // 2. 初始化作弊小抄 (dp 数组)
    定义一维数组 dp[N+1]
    dp[1] = 1
    dp[2] = 2
    
    // 3. 状态转移计算
    FOR i = 3 TO N DO:
        // 核心逻辑：当前阶数 = 上一阶 + 上两阶
        dp[i] = dp[i-1] + dp[i-2]
    END FOR
    
    // 4. 返回终点
    RETURN dp[N]
```





3.考场楼梯变成了收费制。每阶台阶上都有一个收费站，价格由数组 `cost[i]` 给出。

- 规则 1：你依然每次只能向上爬 **1 阶** 或 **2 阶**。
- 规则 2：你一旦站上第 $i$ 阶，就要交钱 `cost[i]` 才能继续往上走。
- 目标：求爬到楼顶（即跨过最后一阶台阶）的**最小总花费**。

```
Algorithm MinCostClimbingStairs(cost, N):
    // cost 是长度为 N 的数组，代表 0 到 N-1 阶的费用
    // N 代表楼顶（即跨过所有台阶）

    // 1. 特判与边界初始化 (Initialization)
    IF N == 0 THEN RETURN 0
    IF N == 1 THEN RETURN cost[0]
    
    定义一维数组 dp[N]
    dp[0] = cost[0]
    dp[1] = cost[1]
    
    // 2. 状态转移计算 (State Transition)
    FOR i = 2 TO N-1 DO:
        // 核心逻辑：从前两阶中挑一个便宜的，加上自己这一阶的钱
        dp[i] = min(dp[i-1], dp[i-2]) + cost[i]
    END FOR
    
    // 3. 返回登顶结果
    // 注意：楼顶是在 N 阶之上，意味着你可以从第 N-1 阶跳上来，也可以从第 N-2 阶跳上来
    // 到达楼顶不需要再交钱了，所以最后一步就是纯粹比较谁更便宜
    RETURN min(dp[N-1], dp[N-2])
```



4.在一个 $N \times M$ 的网格化安全隔离区中，每个节点代表一个路由器。

你需要把一个机密数据包从左上角起点 $(0,0)$ 传输到右下角终点 $(N-1, M-1)$。

每个路由器节点在处理数据包时，都有被敌方侦测到的风险，我们用一个二维数组 `Risk[i][j]` 来表示经过第 $i$ 行、第 $j$ 列路由器的**暴露风险值**。

为了兼顾隐蔽和效率，数据包的路由规则如下：

1. 它可以向**右**传给下一个路由器。
2. 它可以向**下**传给下一个路由器。
3. **（新增特权）** 它还可以直接向右下方（对角线）传给下一个路由器（即从 $(x, y)$ 直接跳到 $(x+1, y+1)$）。

**目标：** 找出一条从起点到终点的路径，使得整个传输过程的**总暴露风险值最小**。

```
Algorithm MinRiskPath(Risk, N, M):
    // 1. 初始化阶段 (Initialization)
    定义一个大小为 N x M 的二维数组 dp[N][M]
    
    // 初始化起点
    dp[0][0] = Risk[0][0]
    
    // 初始化第一行：数据包只能一直向右传
    FOR j = 1 TO M-1 DO:
        dp[0][j] = dp[0][j-1] + Risk[0][j]
    END FOR
    
    // 初始化第一列：数据包只能一直向下传
    FOR i = 1 TO N-1 DO:
        dp[i][0] = dp[i-1][0] + Risk[i][0]
    END FOR
    
    // 2. 状态转移阶段 (State Transition)
    // 从第1行、第1列开始，此时每个节点都有完整的三个方向(上、左、左上)可以参考
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            // 找出三个历史来源中的最小风险值
            min_prev = min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])
            // 加上当前节点的风险值
            dp[i][j] = min_prev + Risk[i][j]
        END FOR
    END FOR
    
    // 3. 返回最终结果
    RETURN dp[N-1][M-1]
```

**时间复杂度：** 算法主要包含两个单独的边界初始化循环，以及一个嵌套的双层循环遍历剩余节点。每个节点的状态计算只需常数时间 $O(1)$，总共计算 $N \times M$ 个节点，因此总时间复杂度为 **$O(N \times M)$**。

**空间复杂度：** 算法额外开辟了一个大小为 $N \times M$ 的二维数组来保存中间状态，因此空间复杂度为 **$O(N \times M)$**。



5.在一个 $N \times M$ 的网格迷宫中，探险家从左上角 `(0, 0)` 走到右下角 `(N-1, M-1)`，只能向右或向下走。 但这次迷宫里有**石头（障碍物）**。如果网格 `Grid[i][j] == 1`，表示这里是平地，走过去能拿到 1 个金币；如果 `Grid[i][j] == 0`，表示这里是**石头**，根本无法进入。 求到达终点能收集到的**最大金币数**（如果根本走不到终点，返回 0）。

```
Algorithm MaxCoinsWithObstacles(Grid, N, M):
    // 0. 极端情况特判
    IF Grid[0][0] == 0 THEN RETURN 0  // 起点就是石头，直接下班
    
    定义二维数组 dp[N][M]，全部初始化为 0
    dp[0][0] = Grid[0][0]
    
    // 1. 初始化第一行 (注意连锁反应！)
    FOR j = 1 TO M-1 DO:
        IF Grid[0][j] == 0 THEN 
            BREAK  // 遇到石头，后面的路全堵死了，保持默认的 0 即可，直接跳出循环
        ELSE
            dp[0][j] = dp[0][j-1] + Grid[0][j]
    END FOR
    
    // 2. 初始化第一列 (同理，向下遇到石头就停止)
    FOR i = 1 TO N-1 DO:
        IF Grid[i][0] == 0 THEN
            BREAK  // 遇到石头，下面的路全堵死了
        ELSE
            dp[i][0] = dp[i-1][0] + Grid[i][0]
    END FOR
    
    // 3. 核心状态转移
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            IF Grid[i][j] == 0 THEN
                dp[i][j] = 0  // 进不去，记为0
            ELSE
                // 如果左边或上面是石头，它们的 dp 值本来就是 0，所以用 max 不受影响
                dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + Grid[i][j]
        END FOR
    END FOR
    
    RETURN dp[N-1][M-1]
```



6.在一个 $N \times M$ 的空白网格中，一个机器人站在左上角的起点 `(0, 0)`。

机器人的目标是走到右下角的终点 `(N-1, M-1)`。

**规则：** 机器人每次只能向**右**走一步，或者向**下**走一步。

**问题：** 机器人从起点走到终点，总共有**多少条不同的路线**？

```
Algorithm UniquePaths(N, M):
    // 1. 定义二维数组
    定义二维数组 dp[N][M]
    
    // 2. 初始化边界：第一行和第一列的走法全都是 1 种
    FOR j = 0 TO M-1 DO:
        dp[0][j] = 1
    END FOR
    FOR i = 0 TO N-1 DO:
        dp[i][0] = 1
    END FOR
    
    // 3. 核心状态转移
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            // 直接将上面的路线数和左边的路线数相加
            dp[i][j] = dp[i-1][j] + dp[i][j-1]
        END FOR
    END FOR
    
    // 4. 返回右下角的终点结果
    RETURN dp[N-1][M-1]
```

如果带障碍物

```
Algorithm UniquePathsWithObstacles(Grid, N, M):
    // 0. 极端情况特判
    // 如果起点本身就是障碍物，那连出发都做不到，直接返回 0
    IF Grid[0][0] == 1 THEN RETURN 0
    
    // 1. 定义二维数组，并默认全部初始化为 0
    定义二维数组 dp[N][M] = {0}
    dp[0][0] = 1
    
    // 2. 初始化第一行 (注意连锁反应！)
    FOR j = 1 TO M-1 DO:
        IF Grid[0][j] == 1 THEN 
            BREAK  // 遇到障碍物，后面的路全堵死了，保持默认的 0 即可
        ELSE
            dp[0][j] = 1
    END FOR
    
    // 3. 初始化第一列 (同理，向下遇到障碍物就停止)
    FOR i = 1 TO N-1 DO:
        IF Grid[i][0] == 1 THEN
            BREAK  // 遇到障碍物，下面的路全堵死了
        ELSE
            dp[i][0] = 1
    END FOR
    
    // 4. 核心状态转移
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            // 严格按照题目设定，1 是死路，0 是平地
            IF Grid[i][j] == 1 THEN
                dp[i][j] = 0   // 此路不通，走法为 0
            ELSE
                dp[i][j] = dp[i-1][j] + dp[i][j-1]
        END FOR
    END FOR
    
    // 5. 返回右下角的终点结果
    RETURN dp[N-1][M-1]
```







7.你在玩一个跳伞寻宝游戏。地图依然是一个 $N \times N$ 的网格，每个房间都有数量不等的金币 `Grid[i][j]`。

- **规则 1（任意起飞）：** 游戏开始时，你可以选择降落在 **第一行（第 0 行）** 的任意一个房间作为起点。
- **规则 2（重力限制）：** 降落后，你每次只能向 **正下方 `(i+1, j)`** 走，或者向 **右下方 `(i+1, j+1)`** 走。
- **目标：** 一路走到最后一行，找出能拿到最多金币的路线。

```
Algorithm MaxFallingGold(Grid, N, M):
    // N 是行数，M 是列数
    定义二维数组 dp[N][M]
    
    // 1. 初始化第一行 (因为可以降落在第一行的任意房间)
    FOR j = 0 TO M-1 DO:
        dp[0][j] = Grid[0][j]
    END FOR
    
    // 2. 核心状态转移 (从第 1 行开始向下逐层降落)
    FOR i = 1 TO N-1 DO:
        FOR j = 0 TO M-1 DO:
            // 处理边界：贴着最左边跳，没有“左上方”这个选项
            IF j == 0 THEN
                dp[i][j] = dp[i-1][j] + Grid[i][j]
            ELSE
                // 正常情况：比较“正上方”和“左上方”谁更大
                dp[i][j] = max(dp[i-1][j], dp[i-1][j-1]) + Grid[i][j]
            END IF
        END FOR
    END FOR
    
    // 3. 在最后一行寻找全局最大值
    // 因为你可以走到最后一行的任意一个房间结束
    max_ans = dp[N-1][0]   // 先假设最左下角的数字是最大的
    FOR j = 1 TO M-1 DO:
        IF dp[N-1][j] > max_ans THEN
            max_ans = dp[N-1][j]  // 遇到更大的就更新
    END FOR
    
    RETURN max_ans
```



8.在一个 $N \times M$ 的迷宫里，你要从起点 `(0,0)` 走到终点 `(N-1, M-1)`，每次只能向右或向下走。

这次要求的是：**走到终点的“最少步数”**（每走一格算 1 步）。

- `Grid[i][j] == 0` 表示平地，可以正常走。
- `Grid[i][j] == 1` 表示坚固的墙壁，正常情况下绝对过不去。
- **【你的特权】**：你手里**有且仅有 1 枚高爆破甲弹**。在整个迷宫的探索中，你可以**炸毁任意 1 面墙壁**（强行跨过去）。

**问题：** 在拥有 1 枚炸弹的情况下，到达终点的最少步数是多少？（如果连炸弹都救不了你，返回 -1）。

```
Algorithm MinStepsWithBomb(Grid, N, M):
    // 1. 初始化三维数组
    定义 dp[N][M][2]，全部初始化为 +∞
    
    // 起点初始化 (假设起点本身是平地)
    dp[0][0][0] = 0   // 站在起点不需要走，步数为 0
    dp[0][0][1] = +∞  // 还没走呢，不可能凭空用掉炸弹
    
    // 2. 初始化第一行 (只能一直向右走，看左边邻居的小抄)
    FOR j = 1 TO M-1 DO:
        IF Grid[0][j] == 0 THEN
            // 平地：继承同维度的历史
            dp[0][j][0] = dp[0][j-1][0] + 1
            dp[0][j][1] = dp[0][j-1][1] + 1
        ELSE
            // 墙壁：必须在这里用炸弹，继承第 0 页的历史
            dp[0][j][0] = +∞
            dp[0][j][1] = dp[0][j-1][0] + 1
        END IF
    END FOR
    
    // 3. 初始化第一列 (只能一直向下走，看上面邻居的小抄)
    FOR i = 1 TO N-1 DO:
        IF Grid[i][0] == 0 THEN
            dp[i][0][0] = dp[i-1][0][0] + 1
            dp[i][0][1] = dp[i-1][0][1] + 1
        ELSE
            dp[i][0][0] = +∞
            dp[i][0][1] = dp[i-1][0][0] + 1
        END IF
    END FOR
    
    // 4. 核心状态转移 (全向查抄)
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            
            // 为了代码清爽，先把两个页面的“历史最优解”算出来
            min_prev_0 = min(dp[i-1][j][0], dp[i][j-1][0]) // 历史从没用过炸弹
            min_prev_1 = min(dp[i-1][j][1], dp[i][j-1][1]) // 历史已经用过炸弹
            
            IF Grid[i][j] == 0 THEN
                // 【当前是平地】：不用炸，直接平移继承
                dp[i][j][0] = min_prev_0 + 1
                dp[i][j][1] = min_prev_1 + 1
            ELSE
                // 【当前是墙壁】：高光时刻！跨页查抄！
                dp[i][j][0] = +∞                // 没炸弹就死路一条
                dp[i][j][1] = min_prev_0 + 1    // 炸碎它！消耗一次机会
            END IF
            
        END FOR
    END FOR
    
    // 5. 结算大结局
    // 能到达终点的最少步数，是“没用过炸弹”和“用过炸弹”两条时间线里的赢家
    ans = min(dp[N-1][M-1][0], dp[N-1][M-1][1])
    
    IF ans >= +∞ THEN
        RETURN -1  // 悲剧：两页小抄都是无穷大，说明炸弹都不够用，走不到终点
    ELSE
        RETURN ans
```



9.你在玩吃金币游戏，只能向右或向下走。求到达终点的**最大收益**。

- 平地 `Grid[i][j]` 是正数（金币）。
- 毒气室 `Grid[i][j]` 是负数（扣分，比如 -10）。
- 铁墙 `Grid[i][j]` 极其坚固，**绝对无法通行**。

```
Algorithm MaxProfitWithTraps(Grid, N, M):
    // 0. 特殊情况判断
    // 如果起点或者终点就是铁墙，直接返回无解
    IF Grid[0][0] 是铁墙 OR Grid[N-1][M-1] 是铁墙 THEN 
        RETURN -无穷
    
    // 1. 定义二维数组
    // 全部初始化为 -无穷，这是一种非常保险的兜底策略
    定义二维数组 dp[N][M]，初始值全设为 -无穷
    
    // 起点初始化
    dp[0][0] = Grid[0][0]
    
    // 2. 初始化第一行 (只能向右走)
    FOR j = 1 TO M-1 DO:
        IF Grid[0][j] 是铁墙 THEN
            BREAK // 遇到铁墙直接停止，后面的格子保持 -无穷 的初始值，彻底封死
        ELSE
            dp[0][j] = dp[0][j-1] + Grid[0][j]
    END FOR
    
    // 3. 初始化第一列 (只能向下走)
    FOR i = 1 TO N-1 DO:
        IF Grid[i][0] 是铁墙 THEN
            BREAK // 同理，遇到铁墙，下面的路彻底封死
        ELSE
            dp[i][0] = dp[i-1][0] + Grid[i][0]
    END FOR
    
    // 4. 核心状态转移
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            IF Grid[i][j] 是铁墙 THEN
                // 核心防线：用 -无穷 剥夺铁墙的竞争资格
                dp[i][j] = -无穷
            ELSE
                // 如果左边和上面都是 -无穷（即被铁墙包围的死胡同），
                // max 算出来也是 -无穷，加上当前 Grid 值后依然极小，符合物理逻辑
                dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + Grid[i][j]
            END IF
        END FOR
    END FOR
    
    // 5. 结果判断
    ans = dp[N-1][M-1]
    IF ans <= -9999999 THEN // 假设 -9999999 足够代表 -无穷
        RETURN "无法到达终点"
    ELSE
        RETURN ans
```



10.经典的走网格（向右/向下），求走到终点的**最大金币数**。 但地图上有一个特殊的单向传送门：如果你走到了房间 `(2, 2)`，你可以选择**花 5 个金币的传送费**，直接瞬移到房间 `(8, 8)`。 当然，你也可以选择不传送，纯靠腿一路从别的地方正常走到 `(8, 8)`，房间 `(8, 8)` 本身地上有 `Grid[8][8]` 个金币。

```
Algorithm MaxProfitWithTeleport(Grid, N, M):
    // 假设 N 和 M 都大于 8，否则这个传送门就不存在了
    
    // 1. 定义二维数组
    定义二维数组 dp[N][M]，初始值设为 0
    dp[0][0] = Grid[0][0]
    
    // 2. 初始化第一行 (只能向右走)
    FOR j = 1 TO M-1 DO:
        dp[0][j] = dp[0][j-1] + Grid[0][j]
    END FOR
    
    // 3. 初始化第一列 (只能向下走)
    FOR i = 1 TO N-1 DO:
        dp[i][0] = dp[i-1][0] + Grid[i][0]
    END FOR
    
    // 4. 核心状态转移
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            
            // 【特殊判定】：如果走到了传送门出口 (8, 8)
            IF i == 8 AND j == 8 THEN
                // 比较三条路：从上面来、从左边来、从(2,2)传送过来（扣除5个手续费）
                max_path = max(dp[7][8], dp[8][7], dp[2][2] - 5)
                dp[8][8] = max_path + Grid[8][8]
                
            // 【普通格子】：正常查小抄
            ELSE
                dp[i][j] = max(dp[i-1][j], dp[i][j-1]) + Grid[i][j]
            END IF
            
        END FOR
    END FOR
    
    // 5. 大结局
    RETURN dp[N-1][M-1]
```



11.就是我们刚才讲的“炸弹人迷宫”题。 唯一的变化是：出题老师觉得 1 颗炸弹太简单了，他给了你 **2 颗炸弹**！也就是你在整个迷宫里，最多可以炸毁 **2 面墙**。求到达终点的最少步数

```
Algorithm MinStepsWithTwoBombs(Grid, N, M):
    // 1. 定义三维数组，包含 3 个页面（0颗、1颗、2颗），全设为 +∞
    定义 dp[N][M][3] = {+∞}
    
    // 起点初始化 (假设起点是平地)
    dp[0][0][0] = 0
    dp[0][0][1] = +∞
    dp[0][0][2] = +∞
    
    // 2. 初始化第一行 (只能向右，看左边邻居)
    FOR j = 1 TO M-1 DO:
        IF Grid[0][j] == 0 THEN // 平地：平移
            dp[0][j][0] = dp[0][j-1][0] + 1
            dp[0][j][1] = dp[0][j-1][1] + 1
            dp[0][j][2] = dp[0][j-1][2] + 1
        ELSE // 墙壁：降维打击
            dp[0][j][0] = +∞
            dp[0][j][1] = dp[0][j-1][0] + 1
            dp[0][j][2] = dp[0][j-1][1] + 1
        END IF
    END FOR
    
    // 3. 初始化第一列 (只能向下，看上面邻居)
    FOR i = 1 TO N-1 DO:
        IF Grid[i][0] == 0 THEN
            dp[i][0][0] = dp[i-1][0][0] + 1
            dp[i][0][1] = dp[i-1][0][1] + 1
            dp[i][0][2] = dp[i-1][0][2] + 1
        ELSE
            dp[i][0][0] = +∞
            dp[i][0][1] = dp[i-1][0][0] + 1
            dp[i][0][2] = dp[i-1][0][1] + 1
        END IF
    END FOR
    
    // 4. 核心状态转移 (把你的推导直接嵌进来)
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            IF Grid[i][j] == 0 THEN
                // 平地：没用炸弹，各过各的独木桥
                dp[i][j][0] = min(dp[i-1][j][0], dp[i][j-1][0]) + 1
                dp[i][j][1] = min(dp[i-1][j][1], dp[i][j-1][1]) + 1
                dp[i][j][2] = min(dp[i-1][j][2], dp[i][j-1][2]) + 1
            ELSE
                // 墙壁：必须用 1 颗炸弹，集体去查“少用 1 颗”的旧账
                dp[i][j][0] = +∞
                dp[i][j][1] = min(dp[i-1][j][0], dp[i][j-1][0]) + 1
                dp[i][j][2] = min(dp[i-1][j][1], dp[i][j-1][1]) + 1
            END IF
        END FOR
    END FOR
    
    // 5. 大结局：货比三家
    // 走到终点可能没用炸弹，可能用了 1 颗，也可能用了 2 颗，挑个最小的
    ans = min(dp[N-1][M-1][0], dp[N-1][M-1][1], dp[N-1][M-1][2])
    
    IF ans >= +∞ THEN
        RETURN -1  // 2颗炸弹都救不了你，烂在迷宫里了
    ELSE
        RETURN ans
```



12.走格子，拿最大收益，一次瞬移(i, j)到(i+1, j+1)，有负权值，只能向右或者向下

```
Algorithm SEU_DiagonalJump_Compact(Grid, N, M):
    
    // 1. 初始化两页小抄，全部铺满 -∞
    定义 dp[N][M][2] = {-∞}
    
    // 起点
    dp[0][0][0] = Grid[0][0]
    dp[0][0][1] = -∞ 
    
    // 2. 初始化第一行 (只能向右，无法发生对角线瞬移)
    FOR j = 1 TO M-1 DO:
        dp[0][j][0] = dp[0][j-1][0] + Grid[0][j]
        dp[0][j][1] = -∞ 
    END FOR
    
    // 3. 初始化第一列 (只能向下，无法发生对角线瞬移)
    FOR i = 1 TO N-1 DO:
        dp[i][0][0] = dp[i-1][0][0] + Grid[i][0]
        dp[i][0][1] = -∞
    END FOR
    
    // 4. 核心状态转移（极简风格）
    FOR i = 1 TO N-1 DO:
        FOR j = 1 TO M-1 DO:
            
            // 第 0 页：纯路人，没用过瞬移，只能从上面或左边来
            dp[i][j][0] = max(dp[i-1][j][0], dp[i][j-1][0]) + Grid[i][j]
            
            // 第 1 页：瞬移者，三种情况大乱斗（继承上面、继承左边、此时此地发动对角线瞬移）
            dp[i][j][1] = max(dp[i-1][j][1], dp[i][j-1][1], dp[i-1][j-1][0]) + Grid[i][j]
            
        END FOR
    END FOR
    
    // 5. 大结局
    RETURN max(dp[N-1][M-1][0], dp[N-1][M-1][1])
```



## 0-1背包

1.你背着一个最大承重为 $W$ 的背包。

宝库里有 $N$ 个物品排成一排。每个物品有两个属性：

- **重量** $w[i]$（占多少空间）
- **价值** $v[i]$（值多少钱）

你的目标是：在不超过背包承重 $W$ 的前提下，装走价值最大的物品组合。每个物品只有一件，你要么装（1），要么不装（0）。

$dp[i][j]$ 的终极含义是： **面对前 $i$ 个物品，在背包容量刚好为 $j$ 的限制下，你能装出的最大总价值。**

```
Algorithm ZeroOneKnapsack(w, v, N, W):
    // w 是重量数组，v 是价值数组，假设下标从 1 开始到 N
    // N 是物品总数，W 是背包最大承重
    
    // 1. 初始化 DP 表格 (由于是 0 开始，大小需开 N+1 行，W+1 列)
    // 默认全填 0 即可，天然满足了第 0 行和第 0 列全为 0 的边界条件
    定义二维数组 dp[N+1][W+1] = {0}
    
    // 2. 核心状态转移
    // i 代表当前正在考虑第 i 个物品
    FOR i = 1 TO N DO:
        
        // j 代表当前假设的背包剩余容量 (从 1 遍历到最大容量 W)
        FOR j = 1 TO W DO:
            
            // 选项 A：这件物品太重了，目前的容量 j 根本装不下
            IF w[i] > j THEN
                // 只能选择不装，完全继承前 i-1 个物品在容量 j 下的最优解
                dp[i][j] = dp[i-1][j]
                
            // 选项 B：能装得下，进入终极二选一
            ELSE
                // 货比三家，取最大值
                dp[i][j] = max(dp[i-1][j]，v[i] + dp[i-1][j - w[i]])
            END IF
            
        END FOR
    END FOR
    
    // 3. 大结局
    // 站在最后一个物品面前，拥有最大容量 W 时，就是我们要的最终答案
    RETURN dp[N][W]
```




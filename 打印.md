# PV专题

## 生产者-消费者

1 **题目描述：**

三个进程 P1、P2、P3 互斥使用一个包含 $N(N>0)$ 个单元的缓冲区。

- **P1**：每次用 `produce()` 生成一个正整数，并用 `put()` 送入缓冲区某一空单元中；
- **P2**：每次用 `getodd()` 从该缓冲区中取出一个**奇数**，并用 `countodd()` 统计奇数个数；
- **P3**：每次用 `geteven()` 从该缓冲区中取出一个**偶数**，并用 `counteven()` 统计偶数个数。

请用**信号量机制**实现这三个进程的同步与互斥活动，并说明所定义信号量的含义。要求用伪代码描述。



解答：

> 信号量定义

- `mutex = 1`: 互斥信号量，用于控制对缓冲区的互斥访问。
- `empty = N`: 资源信号量，表示缓冲区中空单元的数量。
- `odd = 0`: 资源信号量，表示缓冲区中奇数的数量。
- `even = 0`: 资源信号量，表示缓冲区中偶数的数量。

> 伪代码

```c
semaphore mutex = 1;      // 互斥访问缓冲区
semaphore empty = N;      // 缓冲区空槽位
semaphore odd = 0;        // 奇数资源
semaphore even = 0;       // 偶数资源

P1() {
    while(1) {
        num = produce();  // 生成一个正整数
        P(empty);         // 等待空位
        P(mutex);         // 进入临界区
        put();            // 将数字送入缓冲区
        V(mutex);         // 离开临界区
        
        if (num % 2 != 0) 
            V(odd);       // 如果是奇数，通知 P2
        else 
            V(even);      // 如果是偶数，通知 P3
    }
}

P2() {
    while(1) {
        P(odd);           // 等待奇数资源
        P(mutex);         // 进入临界区
        getodd();         // 取出奇数
        V(mutex);         // 离开临界区
        V(empty);         // 释放一个空位
        countodd();       // 统计奇数
    }
}

P3() {
    while(1) {
        P(even);          // 等待偶数资源
        P(mutex);         // 进入临界区
        geteven();        // 取出偶数
        V(mutex);         // 离开临界区
        V(empty);         // 释放一个空位
        counteven();      // 统计偶数
    }
}
```



2 **题目描述：** 

系统中多个生产者进程和多个消费者进程，共享一个能存放 1000 件产品的环形缓冲区（初始为空）。

- **生产者规则**：缓冲区未满时可放入一件产品，否则等待。
- **消费者规则**：缓冲区未空时可取走一件产品，否则等待。
- **特殊要求**：要求**一个消费者进程从缓冲区连续取出 10 件产品后，其他消费者进程才可以取产品**。 请使用信号量 P、V 操作实现进程间的互斥与同步，写出完整过程，并说明所用信号量的含义和初值。



```c
semaphore empty = 1000; // 空位
semaphore full = 0;     // 产品
semaphore mutex_p = 1;  // 生产者之间的互斥
semaphore mutex_c = 1;  // 消费者之间的互斥（关键：保证连续取10件）
semaphore mutex = 1;    // 缓冲区的物理操作互斥

// 生产者进程
producer() {
    while(1) {
        product = produce(); // 生产一件产品
        
        wait(empty);    // 等待空位
        wait(mutex_p);  // 多个生产者抢夺放入权
        wait(mutex);    // 保证对缓冲区操作的原子性
        
        put(product);   // 放入产品
        
        signal(mutex);
        signal(mutex_p);
        signal(full);   // 增加一个产品资源
    }
}

// 消费者进程
consumer() {
    while(1) {
        wait(mutex_c);  // 【关键】抢到这个锁的消费者，可以连续执行下面的循环
        
        // 循环 10 次，连续取 10 件产品
        for(int i = 0; i < 10; i++) {
            wait(full);     // 等待产品资源
            wait(mutex);    // 保证对缓冲区操作的原子性
            
            get();          // 取出一件产品
            
            signal(mutex);
            signal(empty);  // 释放一个空位
            
            consume();      // 消费产品
        }
        
        signal(mutex_c); // 拿完 10 件后，才释放锁，让其他消费者进来
    }
}
```





3 **题目描述：**

 有 A、B 两人通过信箱进行辩论，每个人都从自己的信箱中取得对方的问题。将答案和向对方提出的新问题组成一个邮件放入对方的信箱中。

- **信箱限制**：假设 A 的信箱最多放 $M$ 个邮件，B 的信箱最多放 $N$ 个邮件。
- **初始状态**：初始时 A 的信箱中有 $x$ 个邮件 ($0 < x < M$)，B 的信箱中有 $y$ 个邮件 ($0 < y < N$)。
- **辩论规则**：每取出一个邮件，邮件数减 1。

操作过程描述：

```c
CoBegin
A {
    while(TRUE) {
        从 A 的信箱中取出一个邮件;
        回答问题并提出一个新问题;
        将新邮件放入 B 的信箱;
    }
}
B {
    while(TRUE) {
        从 B 的信箱中取出一个邮件;
        回答问题并提出一个新问题;
        将新邮件放入 A 的信箱;
    }
}
CoEnd
```



代码：

```c
semaphore full_A = x, empty_A = M - x;
semaphore full_B = y, empty_B = N - y;
semaphore mutex_A = 1, mutex_B = 1;

A() {
    while(TRUE) {
        P(full_A);      // 等待 A 信箱有邮件
        P(mutex_A);     // 锁定 A 信箱
        取出邮件;
        V(mutex_A);
        V(empty_A);     // A 信箱多出一个空位

        回答问题并提出一个新问题;

        P(empty_B);     // 等待 B 信箱有空位
        P(mutex_B);     // 锁定 B 信箱
        将新邮件放入 B 信箱;
        V(mutex_B);
        V(full_B);      // B 信箱多出一个邮件
    }
}

B() {
    while(TRUE) {
        P(full_B);      // 等待 B 信箱有邮件
        P(mutex_B);     // 锁定 B 信箱
        取出邮件;
        V(mutex_B);
        V(full_B);      // 错误纠正：取出邮件应释放空位 V(empty_B)
        
        /* 纠正后如下 */
        // V(empty_B);

        回答问题并提出一个新问题;

        P(empty_A);     // 等待 A 信箱有空位
        P(mutex_A);     // 锁定 A 信箱
        将新邮件放入 A 信箱;
        V(mutex_A);
        V(full_A);      // A 信箱多出一个邮件
    }
}
```





4 **题目描述：**

 现有 5 个操作 A、B、C、D 和 E：

1. 操作 C 必须在 A 和 B 完成后执行；
2. 操作 E 必须在 C 和 D 完成后执行。 请使用信号量的 `wait()`，`signal()` 操作（P、V 操作）描述上述操作之间的同步关系，并说明所用信号量及其初值。

> 分析

__核心逻辑分析（前驱图绘制）__

为了理清逻辑，我们先画出这些操作的依赖关系（前驱图）：

- **A → C** (A 完成后 C 才能开始)
- **B → C** (B 完成后 C 才能开始)
- **C → E** (C 完成后 E 才能开始)
- **D → E** (D 完成后 E 才能开始)

可以看到：

- **A 和 B** 是并列的，没有任何前驱，可以直接开始。
- **D** 也是并列的，虽然它和 E 有关，但在执行前没有约束，可以和 A、B 同时开始。
- **C** 是一个“同步点”，需要等 A 和 B 都发出信号。
- **E** 是最后的“同步点”，需要等 C 和 D 都发出信号。

> 代码
> `S_ac = 0`: 表示 A 已完成。
>
> `S_bc = 0`: 表示 B 已完成。
>
> `S_ce = 0`: 表示 C 已完成。
>
> `S_de = 0`: 表示 D 已完成。

```c
semaphore S_ac = 0, S_bc = 0, S_ce = 0, S_de = 0;

CoBegin
Process_A() {
    完成操作 A;
    V(S_ac); // 告诉 C，A 做好了
}

Process_B() {
    完成操作 B;
    V(S_bc); // 告诉 C，B 做好了
}

Process_C() {
    P(S_ac); // 等 A
    P(S_bc); // 等 B
    完成操作 C;
    V(S_ce); // 告诉 E，C 做好了
}

Process_D() {
    完成操作 D;
    V(S_de); // 告诉 E，D 做好了
}

Process_E() {
    P(S_ce); // 等 C
    P(S_de); // 等 D
    完成操作 E;
}
CoEnd
```



5 **题目描述：**

系统运行有三个进程：输入进程、计算进程和打印进程，它们协同完成工作。

- 输入进程和计算进程之间共用缓冲区 **buffer1**；
- 计算进程和打印进程之间共用缓冲区 **buffer2**。
- **输入进程**：接收外部数据放入 buffer1 中；
- **计算进程**：从 buffer1 中取出数据进行计算，然后将结果放入 buffer2；
- **打印进程**：从 buffer2 取出数据打印输出。

用算法描述这三个进程的工作情况，并用 wait 和 signal 原语实现其同步操作。

> 分析

我们需要理清两对“生产-消费”关系：

1. **第一对：输入进程 (Producer) $\rightarrow$ buffer1 $\rightarrow$ 计算进程 (Consumer)**
2. **第二对：计算进程 (Producer) $\rightarrow$ buffer2 $\rightarrow$ 打印进程 (Consumer)**

**注意**：题目没有明确说明缓冲区的大小。在 408 考研大题中，如果没有特殊说明，通常假设缓冲区容量为 **1**（或者理解为单一变量）。如果假设容量为 $N$，则需要多出 `empty` 和 `full` 的计数逻辑。为了严谨，我们按照通用的容量为 $N$ 的逻辑来编写。

> 代码
>
> **针对 buffer1：**
>
> - `empty1 = N1`: buffer1 的空位（初值为缓冲区大小）。
> - `full1 = 0`: buffer1 中的数据量。
> - `mutex1 = 1`: 对 buffer1 的互斥访问。
>
> **针对 buffer2：**
>
> - `empty2 = N2`: buffer2 的空位。
> - `full2 = 0`: buffer2 中的数据量。
> - `mutex2 = 1`: 对 buffer2 的互斥访问。

```c
// 假设 buffer1 容量为 N1，buffer2 容量为 N2
semaphore empty1 = N1; // buffer1 空位，初值为 N1
semaphore full1 = 0;   // buffer1 满位（产品数），初值为 0
semaphore mutex1 = 1;  // buffer1 互斥锁

semaphore empty2 = N2; // buffer2 空位，初值为 N2
semaphore full2 = 0;   // buffer2 满位（产品数），初值为 0
semaphore mutex2 = 1;  // buffer2 互斥锁

void Input_Process() {
    while(TRUE) {
        data = 从外部接收数据; // 生产数据
        
        P(empty1);    // 申请 buffer1 的一个空位
        P(mutex1);    // 锁定 buffer1 临界区
        
        put_to_buffer1(data); // 存入数据
        
        V(mutex1);    // 释放 buffer1 临界区
        V(full1);     // 增加 buffer1 的产品计数，唤醒计算进程
    }
}

void Compute_Process() {
    while(TRUE) {
        // --- 从 buffer1 取数据 ---
        P(full1);     // 等待 buffer1 有数据
        P(mutex1);    // 锁定 buffer1 临界区
        
        data = get_from_buffer1(); // 取出数据
        
        V(mutex1);    // 释放 buffer1 临界区
        V(empty1);    // 增加 buffer1 的空位计数，唤醒输入进程

        // --- 中间处理过程 ---
        result = compute(data); 

        // --- 向 buffer2 放结果 ---
        P(empty2);    // 申请 buffer2 的一个空位
        P(mutex2);    // 锁定 buffer2 临界区
        
        put_to_buffer2(result); // 存入结果
        
        V(mutex2);    // 释放 buffer2 临界区
        V(full2);     // 增加 buffer2 的产品计数，唤醒打印进程
    }
}

void Print_Process() {
    while(TRUE) {
        P(full2);     // 等待 buffer2 有数据
        P(mutex2);    // 锁定 buffer2 临界区
        
        result = get_from_buffer2(); // 取出结果
        
        V(mutex2);    // 释放 buffer2 临界区
        V(empty2);    // 增加 buffer2 的空位计数，唤醒计算进程
        
        print(result); // 打印输出
    }
}
```



6 **题目描述：**

 桌上有一空盘，允许存放一只水果。爸爸可向盘中放苹果，也可向盘中放桔子，儿子专等吃盘中的桔子，女儿专等吃盘中的苹果。规定当盘空时一次只能放一只水果供吃者取用，请用 P、V 原语实现爸爸、儿子、女儿三个并发进程的同步。

> 分析

我们需要理清进程之间的**同步**与**互斥**关系：

1. **同步关系 1（盘子空间）**：爸爸放水果的前提是盘子必须为空。
2. **同步关系 2（苹果资源）**：女儿取苹果的前提是盘子里必须有苹果。
3. **同步关系 3（桔子资源）**：儿子取桔子的前提是盘子里必须有桔子。
4. **互斥关系**：虽然题目中盘子容量为 1，隐含了互斥，但在复杂的同步问题中，对共享资源（盘子）的操作通常需要互斥信号量保护。不过在容量为 1 的简单情况下，同步信号量已能起到互斥作用。

> 代码
>
> `plate = 1`: 表示盘子是否为空（初始为 1，代表可以放一个水果）。
>
> `apple = 0`: 表示盘子中苹果的数量（初始为 0）。
>
> `orange = 0`: 表示盘子中桔子的数量（初始为 0）。
>
> （可选）`mutex = 1`: 用于对盘子操作的互斥访问。

```c
semaphore plate = 1;  // 盘子里的空位
semaphore apple = 0;  // 苹果的数量
semaphore orange = 0; // 桔子的数量
semaphore mutex = 1;  // 互斥访问盘子

// 爸爸进程
void father() {
    while(TRUE) {
        prepare_fruit(); // 准备水果（苹果或桔子）
        
        P(plate);        // 等待盘子变空
        P(mutex);        // 进入临界区
        
        if (is_apple) {
            put_apple(); // 放入苹果
            V(mutex);
            V(apple);    // 通知女儿
        } else {
            put_orange(); // 放入桔子
            V(mutex);
            V(orange);   // 通知儿子
        }
    }
}

// 女儿进程
void daughter() {
    while(TRUE) {
        P(apple);        // 等待苹果
        P(mutex);        // 进入临界区
        
        take_apple();    // 取走苹果
        
        V(mutex);
        V(plate);        // 释放盘子空间，通知爸爸
        eat_apple();     // 吃苹果
    }
}

// 儿子进程
void son() {
    while(TRUE) {
        P(orange);       // 等待桔子
        P(mutex);        // 进入临界区
        
        take_orange();   // 取走桔子
        
        V(mutex);
        V(plate);        // 释放盘子空间，通知爸爸
        eat_orange();    // 吃桔子
    }
}
```



7 提取文字

**设有一个可以装A、B两种物品的仓库，其容量无限大，但要求仓库中A、B两种物品的数量满足下述不等式：$-M \le A物品数量 - B物品数量 \le N$ 其中M和N为正整数。试用信号量和PV操作描述A、B两种物品的入库过程。**

> 分析

该问题的核心在于通过信号量来约束 $A$ 和 $B$ 两种物品的数量差。我们可以将不等式拆解为两个约束条件：

1. **$A - B \le N$**：即 $A \le B + N$。这表示 $A$ 的数量不能比 $B$ 多出超过 $N$ 个。换句话说，入库一个 $A$ 之前，必须确保还有“多出 $N$ 个”的额度。
2. **$A - B \ge -M$**：即 $B - A \le M$ 或 $B \le A + M$。这表示 $B$ 的数量不能比 $A$ 多出超过 $M$ 个。换句话说，入库一个 $B$ 之前，必须确保还有“多出 $M$ 个”的额度。

> 代码
>
> **`Sa`**：表示允许 $A$ 比 $B$ 多入库的数量额度，初始值为 **$N$**。
>
> **`Sb`**：表示允许 $B$ 比 $A$ 多入库的数量额度，初始值为 **$M$**。

```c
semaphore Sa = N;    // A比B领先的剩余额度
semaphore Sb = M;    // B比A领先的剩余额度
semaphore mutex = 1; // 仓库入口的互斥锁

void process_A() {
    while(true) {
        P(Sa);         // 检查 A-B 是否将超过 N
        P(mutex);      // 互斥进入仓库
        put_A();       // 入库操作
        V(mutex);      // 退出仓库
        V(Sb);         // 增加 B 相对 A 的可入库额度
    }
}

void process_B() {
    while(true) {
        P(Sb);         // 检查 B-A 是否将超过 M
        P(mutex);      // 互斥进入仓库
        put_B();       // 入库操作
        V(mutex);      // 退出仓库
        V(Sa);         // 增加 A 相对 B 的可入库额度
    }
}
```





## 哲学家进餐

用资源限制人数

1、有 $n(n \ge 3)$ 位哲学家围坐在一张圆桌边，每位哲学家交替地就餐和思考。在圆桌中心有 $m(m \ge 1)$ 个碗，每两位哲学家之间有 1 根筷子。每位哲学家必须取到一个碗和两侧的筷子之后，才能就餐，进餐完毕，将碗和筷子放回原位，并继续思考。为使尽可能多的哲学家同时就餐，且防止出现死锁现象。请使用信号量的 P、V 操作（wait()、signal() 操作）描述上述过程中的互斥与同步，并说明所用信号量及初值的含义。

分析：

1. **资源需求**：每位哲学家需要 **1个碗** 和 **2根筷子**（左手边和右手边各一根）。
2. **死锁预防**：如果所有哲学家同时拿起左边的筷子，或者所有哲学家同时拿到了碗但抢不到筷子，就会发生死锁。
3. **防止死锁的策略**：限制同时进入餐厅（尝试进餐）的哲学家人数。只要保证同时尝试进餐的人数 $k \le n-1$ 且 $k \le m$，就能打破循环等待。

代码：

```c
// 信号量定义
semaphore chopstick[n] = {1, 1, ..., 1};
semaphore bowl = m;
semaphore room = min(n-1, m); // 限制进餐人数，防止死锁

void philosopher(int i) {
    while (true) {
        think();            // 哲学家思考

        wait(room);         // 限制同时进餐的人数，最多允许 n-1 位
        wait(bowl);         // 申请一个碗
        wait(chopstick[i]); // 申请左手边的筷子
        wait(chopstick[(i + 1) % n]); // 申请右手边的筷子

        eat();              // 进餐

        signal(chopstick[i]); // 释放左手边的筷子
        signal(chopstick[(i + 1) % n]); // 释放右手边的筷子
        signal(bowl);         // 释放碗
        signal(room);         // 离开餐厅，允许他人尝试
    }
}
```



## 服务员-顾客

1、某银行提供 1 个服务窗口和 10 个供顾客等待的座位。顾客到达银行时，若有空座位，则到取号机上领取一个号，等待叫号。取号机每次仅允许一位顾客使用。当营业员空闲时，通过叫号选取一位顾客，并为其服务。顾客和营业员的活动过程描述如下：

```c
cobegin {
    process 顾客 i {
        从取号机获取一个号码；
        等待叫号；获取服务；
    }
    process 营业员 {
        while (TRUE) {
            叫号；
            为客户服务；
        }
    }
} coend
```

问题分析:

**座位资源**：只有 10 个。如果满了，顾客直接离开（隐含条件）。

**取号机互斥**：同一时间只能有一个顾客取号。

**同步关系**：

- **顾客等叫号**：营业员不叫号，顾客不能接受服务。
- **营业员等顾客**：如果没有顾客，营业员必须休息（阻塞）。

代码：

```c
semaphore empty_seats = 10;    // 空座位
semaphore mutex_machine = 1;   // 取号机互斥
semaphore customers = 0;       // 等待的顾客
semaphore service = 0;         // 叫号信号

cobegin {
    process 顾客 i {
        P(empty_seats);        // 检查并占用一个座位
        
        P(mutex_machine);      // 互斥使用取号机
        从取号机获取一个号码；
        V(mutex_machine);      // 释放取号机
        
        V(customers);          // 告诉营业员：有人等服务了
        P(service);            // 等待叫号（在这里阻塞）
        获取服务；
        
        V(empty_seats);        // 服务结束，离开并释放座位
    }

    process 营业员 {
        while (TRUE) {
            P(customers);      // 如果没顾客，就休息
            叫号；              // 营业员空闲，准备叫号
            V(service);        // 发出服务信号，唤醒一位顾客
            为客户服务；
        }
    }
} coend
```





2、假设一个系统中有三个抽烟者进程，每个抽烟者不断地卷烟并抽烟。抽烟者卷起并抽掉一颗烟需要有三种材料：烟草、纸和胶水。一个抽烟者有烟草，一个有纸，另一个有胶水。系统中还有两个供应者进程，它们无限地供应所有三种材料，但每次仅轮流提供三种材料中的两种。得到缺失的两种材料的抽烟者在卷起并抽掉一颗烟后会发信号通知供应者，让它继续提供另外的两种材料。这一过程重复进行。

分析：

**资源关系**：

- 抽烟者 A：自带烟草，需要（纸 + 胶水）。
- 抽烟者 B：自带纸，需要（烟草 + 胶水）。
- 抽烟者 C：自带胶水，需要（烟草 + 纸）。

**供应者逻辑**：随机（或轮流）投放三种组合之一：(纸, 胶水)、(烟草, 胶水)、(烟草, 纸)。

**同步逻辑**：

- 供应者放好材料后，要通知对应的抽烟者。
- 抽烟者抽完后，要通知供应者继续投放。



信号量定义:

| **信号量**   | **初值** | **含义**                                                     |
| ------------ | -------- | ------------------------------------------------------------ |
| **`S1`**     | **0**    | 同步信号量：代表桌上有（纸 + 胶水），通知抽烟者 A。          |
| **`S2`**     | **0**    | 同步信号量：代表桌上有（烟草 + 胶水），通知抽烟者 B。        |
| **`S3`**     | **0**    | 同步信号量：代表桌上有（烟草 + 纸），通知抽烟者 C。          |
| **`finish`** | **1**    | 同步信号量：代表抽烟是否完成，供应者据此决定是否投放下一组。 |



代码：

```c
semaphore S1 = 0, S2 = 0, S3 = 0;
semaphore finish = 1; 
int i = 0; // 用于实现轮流供应

// 供应者进程
process Provider {
    while (TRUE) {
        P(finish); // 等待抽烟者抽完
        if (i == 0) {
            V(S1); // 投放：纸 + 胶水
        } else if (i == 1) {
            V(S2); // 投放：烟草 + 胶水
        } else if (i == 2) {
            V(S3); // 投放：烟草 + 纸
        }
        i = (i + 1) % 3; // 轮流逻辑
    }
}

// 抽烟者 A (拥有烟草)
process Smoker_A {
    while (TRUE) {
        P(S1); // 拿到纸和胶水
        卷烟;
        抽烟;
        V(finish); // 通知供应者可以继续了
    }
}

// 抽烟者 B (拥有纸)
process Smoker_B {
    while (TRUE) {
        P(S2); // 拿到烟草和胶水
        卷烟;
        抽烟;
        V(finish);
    }
}

// 抽烟者 C (拥有胶水)
process Smoker_C {
    while (TRUE) {
        P(S3); // 拿到烟草和纸
        卷烟;
        抽烟;
        V(finish);
    }
}
```





3、面包师有很多面包，由 $n$ 个销售人员推销。每个顾客进店后取一个号，并且等待叫号，当一个销售人员空闲下来时，就叫下一个号。试设计一个使销售人员和顾客同步的算法。

问题分析：

**取号互斥**：虽然题目没明说，但现实中多个顾客不能同时取同一个号，需要一个互斥信号量（或利用变量自增）。

**多服务者同步**：

- **顾客**：取号 $\rightarrow$ 告诉销售人员有人在等 $\rightarrow$ 等待被叫。
- **销售人员**：等待有顾客 $\rightarrow$ 叫号 $\rightarrow$ 服务。

**注意点**：面包师“有很多面包”意味着面包资源充足，不需要考虑面包的生产同步，重点在于**顾客与销售人员的匹配**。



信号量定义

| **信号量**          | **初值** | **含义**                                               |
| ------------------- | -------- | ------------------------------------------------------ |
| **`mutex_get_num`** | **1**    | 互斥信号量：保护取号过程（防止两个顾客取到同一个号）。 |
| **`customers`**     | **0**    | 资源信号量：代表正在等待叫号的顾客数量。               |
| **`sales`**         | **n**    | 资源信号量：代表当前空闲的销售人员数量。               |



代码：

```c
semaphore mutex_get_num = 1; // 互斥取号
semaphore customers = 0;      // 等待的顾客数
semaphore sales = 0;          // 这里的 0 代表等待被叫号的信号，
                              // 也可以设为 n 代表空闲销售员，但 408 标准解法通常如下：

// 顾客进程
void Customer() {
    P(mutex_get_num);
    取号;
    V(mutex_get_num);

    V(customers);             // 增加一名等待的顾客
    P(sales);                 // 等待销售人员叫号
    买面包;
}

// 销售人员进程 (n 个进程并行)
void Seller() {
    while (true) {
        P(customers);         // 如果没顾客，销售人员阻塞/休息
        叫号;
        V(sales);             // 叫到一个号，唤醒一名顾客
        卖面包;
    }
}
```

拓展：

什么时候用 `while(true)`，什么时候不用？

**必须用 `while(true)` 的情况（服务者/生产者）：** 比如 **销售员（Seller）**、**营业员**、**供应者**。 这些角色在现实中是“上班族”。他们处理完一个顾客，不能就地结束进程，必须回到开头等待下一个顾客。如果不加 `while`，销售员卖完一个面包进程就销毁了，店里就没人干活了。

**通常不用 `while(true)` 的情况（消费者/任务）：** 比如 **顾客（Customer）**、**哲学家（Philosopher）**（注：哲学家有时会用 `while` 模拟不断思考/进餐的过程，但 408 考研中，顾客通常进店一次、买完、走人，所以不加 `while`）。 每一个“顾客”都是一个独立的进程实例。



## 读者-写者

1.读者并行,写者间互斥

变量定义

```
int rcount = 1;
semaphore rmutex = 1;
semaphore wmutex = 1;
semaphore mutex = 1;
```



代码：

```c
process reader() {
    P(mutex);              // 闸机锁：保证写者可以排队，实现公平性
    P(rmutex);             // 变量锁：保护 rcount 变量
    if (rcount == 0)       // 如果是第一个读者
        P(wmutex);         // 锁定写者，禁止写操作
    rcount++;              // 读者人数加 1
    V(rmutex);             // 释放变量锁
    V(mutex);              // 离开闸机，允许下一个读或写排队

    do_reading;            // 执行读操作

    P(rmutex);             // 读完回来，再次申请变量锁
    rcount--;              // 读者人数减 1
    if (rcount == 0)       // 如果是最后一个离开的读者
        V(wmutex);         // 释放写者锁，允许写操作
    V(rmutex);             // 释放变量锁
}

process writer() {
    P(mutex);              // 闸机锁：抢占排队位置，挡住后续读者
    P(wmutex);             // 写者锁：保证写操作互斥（且与读者互斥）

    do_writing;            // 执行写操作

    V(wmutex);             // 释放写者锁
    V(mutex);              // 离开闸机，恢复排队秩序
}
```



2、一个主修动物行为学、辅修计算机科学的学生参加了一个课题，调查花果山的猴子是否能被教会理解死锁。他找到一处峡谷，横跨峡谷拉了一根绳索（假设为南北方向），这样猴子就可以攀着绳索越过峡谷。只要它们朝着相同的方向，同一时刻可以有多只猴子通过。但是如果在相反的方向上同时有猴子通过则会发生死锁（这些猴子将被卡在绳索中间，假设这些猴子无法在绳索上从另一只猴子身上翻过去）。如果一只猴子想越过峡谷，它必须看当前是否有别的猴子在逆向通过。请使用 PV 操作来解决该问题。

分析：

**同向不互斥**：多只向北的猴子可以同时在绳索上，多只向南的猴子也可以。

**异向互斥**：当有向北的猴子在绳索上时，向南的猴子必须等待，反之亦然。

**临界资源管理**：第一只进入绳索的猴子需要“锁住”方向，最后一只离开绳索的猴子需要“释放”方向。

变量设置

`countN`, `countS`: 整型变量，分别记录当前在绳索上向北和向南的猴子数量（初值均为 0）。

`mutexN`, `mutexS`: 信号量，用于互斥地访问 `countN` 和 `countS` 变量（初值均为 1）。

`rope`: 信号量，代表绳索的使用权，用于实现南北方向的互斥（初值均为 1）。

代码：

```c
process MonkeyToNorth {
    P(mutexN);          // 锁定计数器 countN
    if (countN == 0) {  // 如果是第一只向北的猴子
        P(rope);        // 尝试占领绳索（若此时有向南的猴子，则等待）
    }
    countN++;           // 增加向北猴子的计数
    V(mutexN);          // 释放计数器锁定

    /* 猴子正在过绳索 */
    过绳索...

    P(mutexN);          // 锁定计数器 countN
    countN--;           // 离开一只猴子
    if (countN == 0) {  // 如果是最后一只离开的向北猴子
        V(rope);        // 释放绳索使用权，允许向南的猴子通过
    }
    V(mutexN);          // 释放计数器锁定
}

process MonkeyToSouth {
    P(mutexS);          // 锁定计数器 countS
    if (countS == 0) {  // 如果是第一只向南的猴子
        P(rope);        // 尝试占领绳索
    }
    countS++;
    V(mutexS);

    /* 猴子正在过绳索 */
    过绳索...

    P(mutexS);
    countS--;
    if (countS == 0) {  // 如果是最后一只离开的向南猴子
        V(rope);        // 释放绳索使用权
    }
    V(mutexS);
}
```



拓展：**为了防止某一个方向的猴子过多导致另一个方向“饥饿”（永远等不到过河机会），我们需要引入**“轮转机制”**和**“配额管理”**。通过增加一个**转向信号量**和一个**计数限制**来解决。

信号量：

**互斥与同步信号量**：

- `rope`: 绳索使用权（初值 1）。
- `turn`: 轮流信号量，确保方向切换的公平性，防止一个方向长期霸占（初值 1）。
- `mutexN`, `mutexS`: 保护各自方向的计数器（初值 1）。

**计数器**：

- `countN`, `countS`: 当前绳索上向北/南的猴子数量。
- `waitingN`, `waitingS`: 正在排队等候的猴子数量（用于判断是否需要强行切换）。



为了实现“最多 10 个”且“不到 10 个也能切换”的逻辑，我们可以采用**“分批过场”**的策略：

**上限控制（最多10个）**： 当 `countN` 达到 10 时，当前猴子**不再执行 `V(turn)`**。这意味着后续同方向的猴子会被阻塞在 `P(turn)` 处，绳索入口被物理切断。

**自动切换（不到10个怎么办）**：

- 如果北向只有 3 只猴子，它们过完后 `countN` 变为 0，最后一只猴子会执行 `V(rope)` 和 `V(turn)`。
- 此时，如果南向有猴子在等，它会立刻通过 `P(turn)` 和 `P(rope)` 获得通行权。
- 这就解决了“数量不足 10 个也能切换”的问题。

**防止饥饿**： 通过 `turn` 信号量，我们保证了即使北向有源源不断的猴子，只要当前批次（10个）走完，绳索控制权就必须重新竞争。在 `V(turn)` 释放后，排队最久的南向猴子有极高概率抢到优先权。

```c
process MonkeyToNorth {
    P(turn);            // 检查是否轮到北向猴子走
    P(mutexN);
    if (countN == 0) {  // 该方向第一只猴子
        P(rope);        // 真正锁定绳索
    }
    countN++;
    
    // 逻辑控制：如果当前过桥人数达到10个，或者南边有猴子在等且本批次已足够
    if (countN < 10) {
        V(turn);        // 只要没满10个，允许同向的下一只猴子进入
    } else {
        // 达到10个了，不执行V(turn)，封锁入口，直到这批全部走完
    }
    V(mutexN);

    /* 猴子过绳索 */
    过绳索...

    P(mutexN);
    countN--;
    if (countN == 0) {  // 这一批最后一只猴子走完了
        V(rope);        // 释放绳索给南向
        V(turn);        // 重新开启入口（此时南向猴子有机会抢到turn）
    }
    V(mutexN);
}
```



3、假设一个录像厅有 0, 1, 2 三种不同的录像片可由观众选择放映，录像厅的放映规则为：

- 任一时刻最多只能放映一种录像片，正在放映的录像片是自动循环放映的，最后一个观众主动离开时结束当前录像片的放映；
- 选择当前正在放映的录像片的观众可立即进入，允许同时有多位选择同一种录像片的观众同时观看，同时观看的观众数量不受限制；
- 等待观看其他录像片的观众按到达顺序排队，当一种新的录像片开始放映时，所有等待观看该录像片的观众可依次序进入录像厅同时观看。用一个进程代表一个观众。
- 要求：用信号量方法 PV 实现，并给出信号量定义和初值。



问题分析

**三种互斥状态**：录像厅在同一时间只能处于“放映 0”、“放映 1”、“放映 2”或“空闲”这四种状态之一。不同种类的录像片之间是**互斥**的。

**同类兼容**：想看同一种片的观众可以**同时**进入。

**服务公平性**：题目要求按“到达顺序排队”，这意味着我们需要一个信号量来维持公平竞争，防止某种片的观众过多导致其他片一直无法放映（防止饥饿）。

**首尾控制**：每种片的第一个观众负责“开场（锁定录像厅）”，最后一个观众负责“散场（释放录像厅）”。



解决方案

1. 信号量定义

- `mutex[3]`: 数组，`mutex[0], mutex[1], mutex[2]` 初值均为 **1**。用于互斥地访问对应录像片的计数器 `count[i]`。
- `hall`: 信号量，初值为 **1**。代表录像厅的放映权，保证同一时刻只有一种片子在放。
- `queue`: 信号量，初值为 **1**。用于实现“按到达顺序排队”，防止新来的同类观众不断“插队”导致其他片等待过久。

2. 变量定义

- `count[3]`: 数组，初值均为 **0**。`count[i]` 记录当前正在看第 `i` 种片的观众人数。

3. PV 操作算法（以第 $i$ 种片的观众进程为例）

```c
// 观众进程 i (i = 0, 1, 2)
process Viewer_i {
    /* --- 入场阶段 --- */
    P(queue);              // 进入排队队列，保证公平性
    P(mutex[i]);           // 锁定自己的计数器
    if (count[i] == 0) {   // 如果我是看这种片的第一个观众
        P(hall);           // 申请录像厅放映权（如果厅里有别的片，则在此等待）
    }
    count[i]++;            // 观众人数加 1
    V(mutex[i]);           // 释放计数器锁
    V(queue);              // 离开排队队列，允许下一个（可能是其他种类）观众排队

    /* --- 观看阶段 --- */
    正在观看录像片 i ...

    /* --- 退场阶段 --- */
    P(mutex[i]);           // 锁定计数器
    count[i]--;            // 观众离开
    if (count[i] == 0) {   // 如果我是最后一个离开的观众
        V(hall);           // 结束当前片子的放映，释放录像厅放映权
    }
    V(mutex[i]);           // 释放计数器锁
}
```



## 其他问题

1、设公共汽车上，司机和售票员的活动分别如下：

- **司机的活动：** 启动车辆；正常行车；到站停车。
- **售票员的活动：** 关车门；售票；开车门。

在汽车不断地到站、停车、行驶过程中，这两个活动有什么同步关系？用信号量和 PV 操作实现它们的同步。

>  逻辑关系分析

司机与售票员之间存在两个关键的**同步约束**：

- **启动约束：** 司机必须等到售票员**关好车门**后才能**启动车辆**。
- **停车约束：** 售票员必须等到司机**停车**后才能**打开车门**。

> 信号量设置

为了实现上述同步，我们需要设置两个初值为 0 的信号量：

- `door_closed`：表示车门是否已关好。初值为 0。
- `stop_done`：表示汽车是否已到站停稳。初值为 0。

> PV 操作实现

```c
semaphore door_closed = 0; // 标识门是否关好
semaphore stop_done = 0;   // 标识是否已停车

// 司机进程
Driver() {
    while(true) {
        P(door_closed);    // 等待售票员关门（请求信号）
        启动车辆;
        正常行车;
        到站停车;
        V(stop_done);      // 告诉售票员已经停车（发出信号）
    }
}

// 售票员进程
Conductor() {
    while(true) {
        关车门;
        V(door_closed);    // 告诉司机门已关好（发出信号）
        售票;
        P(stop_done);      // 等待司机停车（请求信号）
        开车门;
        乘客上下车;         // 循环准备下一次
    }
}
```



2、某寺庙，有小和尚、老和尚若干。庙内有一水缸，由小和尚提水入缸，供老和尚饮用。水缸可容纳10桶水，每次入水、取水仅为1桶，不可同时进行。水取自同一井中，水井径窄，每次只能容纳一个水桶取水。设水桶个数为3个，试用信号量和PV操作给出老和尚和小和尚的活动。

> 核心解析与解题思路

这道“和尚打水”是极为经典的复杂型“生产者-消费者”问题。对于偏好工程实践的思维来说，解答这类题的最佳方式是将其拆解为一条物理流水线，并明确其中的**同步**（依赖协作）和**互斥**（争抢独占）关系。这道题不仅有常规的“缓冲区（水缸）”，还多加了“水桶”和“水井”两个限制条件。我们用上一题的“四步法”来套：

1. **找主体（进程）：**
   - 小和尚（生产者：造水）
   - 老和尚（消费者：消耗水）
2. **找关系（核心约束）：**
   - **互斥 1（水井）：** 井很窄，每次只能一个人用。
   - **互斥 2（水缸）：** 入水和取水不能同时进行。
   - **同步 1（水量）：** 老和尚要喝水，缸里必须有水（等待小和尚倒水）。
   - **同步 2（空位）：** 小和尚要倒水，缸里必须有空位（等待老和尚喝水腾出空间）。
   - **资源池（水桶）：** 无论是打水还是喝水，都必须先拿到水桶（总共 3 个）。
3. **定义信号量：**
   - `mutex_well = 1`（保护水井的互斥锁）
   - `mutex_vat = 1`（保护水缸的互斥锁）
   - `empty = 10`（水缸里的空位数量，同步信号量）
   - `full = 0`（水缸里的水量，同步信号量）
   - `bucket = 3`（水桶数量，资源信号量）

> 高分预警：死锁陷阱

在写代码前，必须避开这道题最大的扣分点：**死锁**。

如果小和尚的逻辑是：**先拿水桶 `P(bucket)` $\rightarrow$ 再看缸里有没有空位 `P(empty)`**，会发生什么？

假设缸已经满了（`empty = 0`），此时 3 个小和尚一人抢到一个水桶。他们来到缸前想倒水，被 `P(empty)` 卡住，抱着水桶死等。而老和尚想喝水腾空位，却因为 3 个水桶都被小和尚霸占了，卡在了 `P(bucket)`。系统彻底死锁。

**破局法则：** 在连续的 P 操作中，**必须先申请同步信号量（看有没有水/空位），再申请资源信号量（拿桶），最后申请互斥信号量（锁缸/锁井）**。

> PV 操作代码实现

```c
// 1. 信号量定义区
semaphore empty = 10;      // 缸中的剩余容量（空位）
semaphore full = 0;        // 缸中的水的桶数
semaphore bucket = 3;      // 庙里总共的水桶数量
semaphore mutex_well = 1;  // 水井互斥锁
semaphore mutex_vat = 1;   // 水缸互斥锁

// 2. 小和尚进程（生产者）
LittleMonk() {
    while(true) {
        // --- 准备阶段 ---
        P(empty);       // 【关键顺序 1】先确认缸里有空位，再行动！防死锁
        P(bucket);      // 拿一个空水桶
        
        // --- 井边操作（临界区 1） ---
        P(mutex_well);  // 霸占水井
        从井中打水;
        V(mutex_well);  // 离开水井
        
        // --- 缸边操作（临界区 2） ---
        P(mutex_vat);   // 霸占水缸
        将水倒入缸中;
        V(mutex_vat);   // 离开水缸
        
        // --- 收尾阶段 ---
        V(full);        // 喊一嗓子：缸里多了一桶水啦！
        V(bucket);      // 把水桶放回原处
    }
}

// 3. 老和尚进程（消费者）
OldMonk() {
    while(true) {
        // --- 准备阶段 ---
        P(full);        // 【关键顺序 2】先确认缸里有水，再行动！防死锁
        P(bucket);      // 拿一个空水桶
        
        // --- 缸边操作（临界区） ---
        P(mutex_vat);   // 霸占水缸
        从缸中取水;
        V(mutex_vat);   // 离开水缸
        
        // --- 收尾阶段 ---
        V(empty);       // 喊一嗓子：缸里空出一个位置啦！
        V(bucket);      // 把水桶放回原处
        
        喝水;           // 喝水属于个人行为，不需要加锁
    }
}
```





3、某博物馆最多可容纳 500 人同时参观，有一个出入口，该出入口一次仅允许一个人通过。参观者的活动描述如下：

```c
cobegin 参观者进程 i: {
    ...
    进门;
    ...
    参观;
    ...
    出门;
    ...
} coend
```

请添加必要的信号量和 P、V（或 wait()、signal()）操作，以实现上述过程中的互斥与同步。要求写出完整的过程，说明信号量的含义并赋初值。

> 逻辑关系分析

**资源限制（同步关系）：** 博物馆是一个容量有限的资源（500人）。当馆内满员时，想进去的人必须等待里面有人出来。

**独占访问（互斥关系）：** 出入口是一个临界资源，同一时刻只能有一个人通过（无论是进还是出）。

> 信号量定义

为了实现上述逻辑，我们需要设置两个信号量：

- `semaphore count = 500;`

  **含义：** 博物馆的剩余容量。初值为 500，表示初始可以进入 500 人。

- `semaphore mutex = 1;`

  **含义：** 出入口的互斥锁。初值为 1，保证进门和出门的操作不会同时发生。

> 完整的 PV 操作过程

```c
semaphore count = 500; // 博物馆空位数，初值为 500
semaphore mutex = 1;   // 出入口互斥信号量，初值为 1

cobegin 参观者进程 i: {
    P(count);      // 检查博物馆是否有空位，有则空位-1，无则等待
    
    P(mutex);      // 申请使用出入口（加锁）
    进门;           // 正在通过门口
    V(mutex);      // 释放出入口（解锁）
    
    参观;           // 参观活动
    
    P(mutex);      // 申请使用出入口（加锁）
    出门;           // 正在通过门口
    V(mutex);      // 释放出入口（解锁）
    
    V(count);      // 离开博物馆，空位+1，提醒外面的人可以进入
} coend
```





4、某进程中有 3 个并发执行的线程 `thread1`、`thread2` 和 `thread3`，其伪代码如下所示。

```c
// 复数的结构类型定义
typedef struct {
    float a;
    float b;
} cnum;

cnum x, y, z; // 全局变量

// 计算两个复数之和
cnum add(cnum p, cnum q) {
    cnum s;
    s.a = p.a + q.a;
    s.b = p.b + q.b;
    return s;
}

// 线程 1
thread1 {
    cnum w;
    w = add(x, y);
    ......
}

// 线程 2
thread2 {
    cnum w;
    w = add(y, z);
    ......
}

// 线程 3
thread3 {
    cnum w;
    w.a = 1;
    w.b = 1;
    z = add(z, w);
    y = add(y, w);
    ......
}
```

**要求：** 请添加必要的信号量和 P、V 操作，要求确保线程互斥访问临界资源，并且最大程度地并发执行。



> 解题核心：寻找“冲突点”

我们可以把这三个全局变量 $x, y, z$ 看作数据库里的三条记录。如果多个线程同时“读写”或“写写”同一条记录，就会产生数据竞争。

**分析每个线程的行为：**

- **thread1：** 读 $x$，读 $y$。
- **thread2：** 读 $y$，读 $z$。
- **thread3：** 先写 $z$（`z = add(z, w)`），再写 $y$（`y = add(y, w)`）。

**锁定临界资源：**

- $x$：只有 thread1 读，没有写者，**不需要锁**。
- $y$：thread1 和 thread2 读，thread3 写。存在“读写冲突”和“写写冲突”，**必须对 $y$ 加锁**。
- $z$：thread2 读，thread3 写。存在“读写冲突”和“写写冲突”，**必须对 $z$ 加锁**。

不要定义一个大锁把 $y$ 和 $z$ 全锁住，而是应该**分别为 $y$ 和 $z$ 定义互斥锁**。这样当 thread3 在改 $z$ 的时候，thread1 依然可以读取 $y$，互不干扰。

> 信号量定义与解答

```c
semaphore mutex_y = 1; // 保护全局变量 y 的互斥锁
semaphore mutex_z = 1; // 保护全局变量 z 的互斥锁

thread1 {
    cnum w;
    P(mutex_y);      // 访问 y 前加锁（x 不需要锁）
    w = add(x, y);
    V(mutex_y);      // 访问后立即释放，提高并发度
    ......
}

thread2 {
    cnum w;
    P(mutex_y);      // 依次申请 y 和 z 的访问权
    P(mutex_z);
    w = add(y, z);
    V(mutex_z);      // 释放顺序建议与申请顺序相反
    V(mutex_y);
    ......
}

thread3 {
    cnum w;
    w.a = 1;
    w.b = 1;
    
    // 对 z 进行写操作
    P(mutex_z);
    z = add(z, w);
    V(mutex_z);
    
    // 对 y 进行写操作
    P(mutex_y);
    y = add(y, w);
    V(mutex_y);
    ......
}
```







## 解题模版

__核心心法：区分“同步”与“互斥”__

在动笔前，在草稿纸上只做一件事：判断动作之间的关系。

- **同步（协作关系）：** 动作有严格的先后顺序（“你没做完 A，我就不能做 B”）。比如：没关门不能开车。
- **互斥（竞争关系）：** 抢夺同一个独占资源（“我在用，你就得等着”）。比如：多个人抢同一个缓冲区写数据。



**PV 操作高分模板与解题四步法**：

__第一步：找主体（划分进程）__

题目里有几类人/事物在独立活动，就写几个 `while(1)` 循环。

- *例：司机和售票员，就是两个独立的进程。*

__第二步：找关系（画前驱图或写条件）__

- 找出谁限制了谁。
- **口诀：“前驱 V，后继 P”**。谁是被等待的先决条件，谁执行完就用 `V` 发信号；谁需要等待条件满足，谁就在动作前用 `P` 请求。

__第三步：定义信号量（**最容易白丢分的地方**）__

**考试拿分秘诀**：不管代码写得多烂，信号量定义清晰，基础分就拿到了。必须包含：**名字 + 初值 + 核心注释**。

- **同步信号量**：初值通常设为 $0$（表示资源尚未产生，或前置动作未完成）。命名建议用业务逻辑，如 `semaphore door_closed = 0;`
- **互斥信号量**：初值通常设为 $1$（表示资源可用）。命名通常带 `mutex`，如 `semaphore mutex = 1;`
- **资源信号量**：初值设为资源数量 $N$（如空闲缓冲区的个数）。命名如 `semaphore empty = N;`

__第四步：套用万能模板写代码__

绝大多数考题都是“生产者-消费者”模型的变体。请将以下骨架刻在脑子里：

```c
// 1. 信号量定义区 (一定要写注释！)
semaphore mutex = 1;      // 互斥信号量，保护共享资源
semaphore empty = N;      // 资源信号量：表示空闲缓冲区的数量
semaphore full = 0;       // 同步信号量：表示已填满缓冲区的数量（初始没东西）

// 2. 进程 A (生产者模型：制造资源)
Producer() {
    while(1) {
        生产一个产品;
        
        P(empty);   // 1. 先检查有没有空位 (同步 P)
        P(mutex);   // 2. 再加锁 (互斥 P)
        
        将产品放入缓冲区; // 临界区操作
        
        V(mutex);   // 3. 先解锁 (互斥 V)
        V(full);    // 4. 通知消费者有东西了 (同步 V)
    }
}

// 3. 进程 B (消费者模型：消耗资源)
Consumer() {
    while(1) {
        P(full);    // 1. 先检查有没有东西 (同步 P)
        P(mutex);   // 2. 再加锁 (互斥 P)
        
        从缓冲区取出一个产品; // 临界区操作
        
        V(mutex);   // 3. 先解锁 (互斥 V)
        V(empty);   // 4. 通知生产者有空位了 (同步 V)
        
        消费这个产品;
    }
}
```

